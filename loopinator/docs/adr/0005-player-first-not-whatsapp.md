# Player-first, not WhatsApp-first, in v1

Church OS coordination apps start in a chat. Loopinator has to loop gapless through the PA with the screen awake, which no chat handoff covers. v1 is a standalone player with stable `/s/{id}` and `/t/{id}` links. A bot may send those links later. It must never become the thing that starts and stops audio on Sunday.
